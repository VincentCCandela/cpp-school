#include<iostream>
using namespace std;

string factorial(int num);

int main(){

	cout << "Input a number to factorialize" << endl;
	cin >> number;
	factorial(number);

return 0;
}

string factorial(int num){
	if(num == 0 || num == 1){
		return "1";
	}
	else {
		return num
		cout << num << "*" << factorial(num - 1);

}
